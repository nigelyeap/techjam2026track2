# Raw dataset directory (use KuaiRand-Pure.tar.gz instead, or download fresh)
KuaiRand-Pure/data/

# Regeneratable per-experiment feature caches (multi-GB, rebuilt automatically on first run)
experiments/**/.cache_v1_*.pkl
experiments/**/.cache_v2_*.pkl
experiments/**/*.pkl

__pycache__/
*.pyc
.DS_Store

# Stray dataset tarball / download scratch (data/ is downloaded fresh per README, not committed)
KuaiRand-Pure.tar.gz
curl_download.log

# Scratch output from local reproduction runs, not part of the deliverable
make_submission.log
make_submission_new.log
submission.csv.bak_iter63
